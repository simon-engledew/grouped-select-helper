Monkey patches a grouped_select method:

<% form_for @person do |f| %>
  <%= f.grouped_select :favourite_food, ['Ice Creams', [['Strawberry', 1], ['Chocolate', 2]]] %>
<% end %>

Until I make this into a plugin just add this into your initializers directory.
